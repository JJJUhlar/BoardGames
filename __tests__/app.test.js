const app = require('../app')

describe('appTests', () => {
    describe('GET: /api/categories', () => {
        test.todo('endpoint has 200 status')
        test.todo('returns an array of category objects')
        test.todo('category objects have slug, description properties')
        test.todo('handles errors')
    })
})